-- Migration from version 2 to 3
-- Add lastMovedRound to GeneralPlacement to track which generals have moved in the current round
ALTER TABLE GeneralPlacement ADD COLUMN lastMovedRound INTEGER NOT NULL DEFAULT 0;
